{{ $serviceName := var "serviceName" }}
{{ if not $serviceName }} {{ end -}}
{
  "application": "test",
  "pipelines": [
    {
      "application": "test",
      "name": "Dinghy Conditional Test",
      "stages": [
          {
            {{ if eq .RawData.pusher.name "Samtribal" }}
              "name": {{ $serviceName }},
            {{ else }}
              "name": {{ $serviceName }},
            {{ end }}
            "waitTime":  10,
            "type": "wait"
          }
      ]
    }
  ]
}
