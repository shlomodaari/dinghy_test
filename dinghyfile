{{ $serviceName := var "serviceName" }}
{{ if not $serviceName }}serviceName is required.{{ end -}}
{
  "application": "test",
  "pipelines": [
    {
      "application": "test",
      "name": "Dinghy Conditional Test",
      "stages": [
          {
            {{ if eq .RawData.pusher.name "Samtribal" }}
              "name": {{ $serviceName }},
            {{ else }}
              "name": "this_is_false",
            {{ end }}
            "waitTime":  10,
            "type": "wait"
          }
      ]
    }
  ]
}
